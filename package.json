{
  "name": "vakilsetu",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd kjk-vakil-main/vakil-main/frontend && npm install --legacy-peer-deps && npm run build",
    "start": "cd kjk-vakil-main/vakil-main/backend && gunicorn server:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:5000"
  }
}
